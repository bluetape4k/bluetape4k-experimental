# 카탈로그 2026-06-02-00 동기화

## 배경

`bluetape4k-dependencies`는 issue #101 security dependency train 이후
`catalog/2026-06-02-00`을 생성했다.

## 결정

이 저장소의 Maven BOM 기반 카탈로그 wiring은 그대로 유지하면서 중앙
카탈로그의 공유 버전 alias를 동기화한다.

## 결과

Netty, Jackson, Jackson 3, AWS SDK v2에 대한 로컬 카탈로그 drift를
제거했다.

## 검증

- `scripts/sync-shared-versions.py --workspace /Users/debop/work/bluetape4k --check --summary`
- `scripts/sync-dependabot-ignores.py --workspace /Users/debop/work/bluetape4k --check --summary`

## 향후 지침

공유 의존성 alert가 발생하면 로컬 버전 override를 추가하지 말고
`bluetape4k-dependencies`를 먼저 갱신한 뒤 여기의 관리 alias를 동기화한다.
